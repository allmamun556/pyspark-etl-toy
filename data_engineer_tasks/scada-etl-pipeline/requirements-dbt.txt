# dbt has its own dependency graph (jinja2, click, etc.) that can conflict
# with both the Airflow image's pins and the dashboard's fastapi stack, so
# it gets its own image, same reasoning as requirements-dashboard.txt.
dbt-core==1.8.2
dbt-postgres==1.8.2
