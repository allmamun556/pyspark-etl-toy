# --- Core pipeline ---
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
alembic==1.13.3
python-dotenv==1.0.1

# --- Data handling ---
pandas==2.2.3
numpy==1.26.4

# --- External data sources (weather API, IoT buoy) ---
requests==2.32.3

# --- Logging ---
python-json-logger==2.0.7

# --- Testing ---
pytest==8.3.3
pytest-cov==5.0.0
faker==30.3.0

# --- Orchestration (installed separately in the Airflow image via base image,
# listed here so `pip install -r requirements.txt` works in a plain venv too) ---
apache-airflow==2.10.2
